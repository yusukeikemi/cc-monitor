# Claude Code 使用状況アドバイス — 2026-06-10

データソース: `~/.claude/cc-monitor/insights/latest.json`(generatedAt: 2026-06-10T05:43:02Z、新鮮)
および `~/.claude/cc-monitor/quota-history.jsonl`。

## 1. TL;DR — 効果の大きい改善 3 点

1. **キャッシュバスト対策が最優先(本日 $7.71 の無駄 = 本日コストの約 27%)**
   `contextHealth.cacheWastedUSD` が $7.71、`cacheBustCount` が 5 回。415K トークンまで膨らんだ
   単一メガセッションでキャッシュが切れるたび、巨大コンテキスト全体が書込価格で再投入されている。
   → 5 分以上の離席をまたいで同一セッションを続けない。長作業は区切りで `/clear` + ハンドオフ。

2. **巨大プロンプト貼り付けをやめ、ファイル参照に切り替える**
   本日のメインセッションでは `userPrompts` だけで約 104K トークン(16 プロンプト、約 417KB)を
   コンテキストに常駐させており、ツール結果(78K)より大きい。`contextSeries` には 1 ステップで
   246K → 404K の約 158K トークンのジャンプがあり、巨大な貼り付けが原因とみられる。
   → ログ・ファイル内容はプロンプトに貼らず、パスを渡して offset/limit 付き Read で読ませる。
   貼り付けた内容はセッションが続く限り毎ターン課金対象として残り続ける。

3. **サブエージェント委譲がゼロ — 探索系を Explore に逃がす**
   `sidechainOutputTokens: 0`、`subagents: []`。一方で `fullFileReads` 12 回、Read のツール結果が
   30 日で約 147K トークンと最大の消費源。調査・探索をメインコンテキストで行うと、その結果が
   以降全ターンのキャッシュ読込コストに乗り続ける。
   → 広い検索・調査は Explore サブエージェントに委譲し、結論だけ受け取る。

## 2. 現状サマリ

| 項目 | 値 | 出典 |
|------|-----|------|
| 本日コスト | $28.70 | `today.totalCost` |
| 今月コスト | $37.81(=全期間。計測は 6 月開始) | `thisMonth.totalCost` |
| 本日のモデル構成 | Fable 5: $28.63(81 msg)/ Opus 4.8: $0.06(2 msg) | `today.modelBreakdown` |
| コスト内訳(本日) | cacheRead $16.61 / cacheWrite $6.68 / output $5.30 / input $0.11 | `today.costBreakdown` |
| キャッシュヒット率 | 94.6% | `contextHealth.cacheHitRate` |
| クォータ | 5h: 3%(05:11 に 100% 到達歴あり)/ 7d: 10% | `quotaLatest`, quota-history |
| Context Health | status: watch、fillRatio 41.5%(415K / 1M) | `contextHealth` |

本日コストの 58% が cacheRead。これは「巨大コンテキストを長時間維持する」運用の直接的な結果で、
モデル単価よりセッションの持ち方が支配項になっている。

## 3. 診断詳細

### キャッシュ効率 — 要改善(最大のレバー)
- **観測**: `cacheBustCount: 5`、`cacheWastedTokens: 670,455`、`cacheWastedUSD: $7.71`。
  本日の cacheWrite は $6.68 で output($5.30)を上回る。
- **解釈**: キャッシュ TTL(5 分)切れ、またはモデル切替・セッション再開でキャッシュが無効化され、
  415K トークンのコンテキストが繰り返し書込価格で再投入されている。コンテキストが大きいほど
  1 回のバストの被害が大きい。
- **アクション**: (a) 作業を中断するときはセッションも区切る。(b) セッション途中のモデル切替
  (`/model`)を避ける — 本日のセッションは `/model` コマンドで始まっている。(c) コンテキストが
  200K を超えたら `/clear` か新セッションへのハンドオフを検討する。

### コンテキスト運用 — 要改善
- **観測**: peakContextTokens 415,336(1M 枠の 41.5%)、成長速度 1,494 tok/分
  (`growthTokensPerMin`)。構成比は userPrompts 104K > toolResults 78K > toolCalls 34K。
  `fullFileReads: 12`、`maxRepeatedCall: Edit ×10`。
- **解釈**: 1M コンテキストモデルなので上限事故の心配は薄いが、コスト面では「大きく保つこと自体」が
  高くつく。プロンプト貼り付けが最大の構成要素という点が特徴的で、ツール出力よりユーザー入力側の
  ダイエットが効く。
- **アクション**: 大きな資料はファイルとして渡す。同一ファイルへの Edit 連打(10 回)は、
  変更方針を先にまとめてから少ない Edit でまとめて適用すると往復が減る。

### クォータ圧力 — 注意
- **観測**: quota-history で 05:06 に 86% → 05:11 に 100% 到達、05:30 のリセットまで張り付き。
  7 日枠は 10% で余裕。
- **解釈**: 5 時間枠をリセット直前に使い切る形になっており、重いタスクの途中で打ち切られるリスクが
  あった。7 日枠には余裕があるので総量の問題ではなく、バーストの問題。
- **アクション**: 重い長時間タスクは 5 時間枠リセット直後(本日なら 05:30 以降)に開始する。
  cc-monitor のクォータ表示で 80% を超えたら新規の重作業を始めない。

### 思考・出力配分 — 改善余地
- **観測**: `sidechainOutputTokens: 0`(サブエージェント未使用)、`assistantTurns: 493` に対し
  `assistantTextTokensEst: 12,441` と説明テキストは軽量。`mainOutputTokens: 601,020`。
- **解釈**: 出力の大半はツール呼び出し駆動の作業で、すべてメインスレッドが負担している。
  探索フェーズの Read 結果(30 日で 147K トークン)がメインコンテキストに蓄積する構造。
- **アクション**: 「どこに何があるか」系の調査は Explore サブエージェントへ。メインには結論のみ
  返るためコンテキストが肥大しない。

### モデルミックス — 現状は妥当、様子見
- **観測**: 今月は Fable 5 が $28.63、Opus 4.8 が $9.11、Sonnet $0.06。
- **解釈**: 開発主体の使い方では妥当な構成。ただし今日のコストの支配項はモデル選択ではなく
  キャッシュ運用なので、モデルを下げるより上記 1〜2 の方が効く。
- **アクション**: 単純な整形・一括置換のような作業をまとめて行う日があれば Haiku 系や
  サブエージェントへの委譲を検討する程度でよい。

### ツール・エラー — 健全
- **観測**: エラー率 2.6%(6/230)。最大は Read の 2 件、AskUserQuestion 1 件。
- **アクション**: 不要。

## 4. やらなくていいこと(すでに健全な点)

- **キャッシュヒット率 94.6%** は高水準。ヒット率自体の改善は不要で、問題はバスト時の単価被害のみ。
- **baselineTokens 3,618** — CLAUDE.md やシステム設定は軽量。設定ファイルのダイエットは不要。
- **reclaimableTokens 6,529** — ツール出力の絞り込み(offset/limit)で取り戻せる量はわずか。
  ツール出力最適化は優先度低。
- **7 日クォータ 10%** — 総使用量に余裕あり。利用を控える必要はない。
- **ツールエラー率 2.6%** — 使い方の問題なし。

## 5. データの注記

- スナップショット生成: 2026-06-10T05:43:02Z(本レポート作成時点で 24 時間以内)。
- `thisMonth` と `allTime` が一致しており、計測データは 2026 年 6 月分のみ(6/8 開始)。
  傾向分析としては母数が小さい点に留意。
- quota-history は 6 件のみ(2026-06-10 の 05:06〜05:36)。時間帯別の傾向分析には履歴が不足。
- `thinkingTokensEst: 0` は思考トークンが推定に含まれていない可能性があり、出力配分の解釈は概算。
- トークン値の多く(`estimatedTokens` 系)は文字数ベースの推定値。
- プロンプト原文は insights に含まれない。158K トークンのジャンプの正体など、セッション内容の
  深掘りには `/cc-session-review 48a5294b-e64e-42c0-a77f-80ea610c230d` を使用。
